services:
  app:
    build: .
    command: ["llm-management", "serve", "--host", "0.0.0.0", "--port", "8080"]
    env_file:
      - .env
    volumes:
      - ./conf/exoscale.toml:/workspaces/llm-management/conf/exoscale.toml:ro
    ports:
      - "8080:8080"
